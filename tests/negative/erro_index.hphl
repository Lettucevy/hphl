// erro_index.hphl — teste negativo em runtime (Milestone 2)
// Compila normalmente, mas a indexação fora dos limites deve panicar
// (hphl_bounds_check → mensagem em stderr + exit 1).

void Main() {
    int[3] v = { 10, 20, 30 };
    print(v[5]);
}
