struct LoadError {
  string mensagem;
  int codigo;
  LoadError(string mensagem, int codigo) {
    this.mensagem = mensagem;
    this.codigo = codigo;
  }
}

void Main() {
  LoadError le = LoadError(mensagem: "x", 3);
  print(le.codigo, "\n");
}