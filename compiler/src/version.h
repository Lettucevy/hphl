#pragma once

// Versão do compilador (semver). Atualizar a cada release junto com CHANGELOG.md
// e o CHANGELOG.md [Unreleased]. Fonte única de verdade para --version, LSP
// serverInfo e mensagens de erro.
#define HPHL_VERSION "1.0.0"