// dbg_teste7.hphl: Write watchpoints (data breakpoints)
// `wploc` pauses on local change; `wpg` pauses on global change.
int contador = 0;
void Main() {
	int x = 100;
	for (int i = 0; i < 8; i++) {
		contador = contador + 1;
		x = x + 1;
		print(contador);
		print("\n");
	}
}
